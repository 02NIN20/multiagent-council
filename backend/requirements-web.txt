# Web server dependencies (FastAPI + full stack)
# Install with: pip install -r backend/requirements-web.txt
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0
pydantic-settings>=2.0
httpx>=0.27.0
python-dotenv>=1.0.0
openai>=1.44.0
python-multipart>=0.0.12

# Database (optional — only for persistent sessions)
# asyncpg>=0.29.0
# pgvector>=0.3.0
# sqlalchemy[asyncio]>=2.0
