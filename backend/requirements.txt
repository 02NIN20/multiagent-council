# Core — needed for MCP server and agent runtime
mcp>=1.0.0
openai>=1.44.0
pydantic>=2.0
pydantic-settings>=2.0
httpx>=0.27.0
python-dotenv>=1.0.0

# Optional — only if you want PostgreSQL/pgvector for persistent sessions
# asyncpg>=0.29.0
# pgvector>=0.3.0
# sqlalchemy[asyncio]>=2.0
